///scr_changeText(textObj, newText, waitForInput)
textObj = argument0;
newText = argument1;
waitForInput = argument2;
with(textObj){
	timer = 0;
	textDraw = "";
	textCount = 0;	
	text = other.newText;
	inputWait = other.waitForInput;
}
